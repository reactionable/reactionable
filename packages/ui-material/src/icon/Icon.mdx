import { Meta, Controls, Primary, Stories, Title, Subtitle } from "@storybook/blocks";
import * as IconStories from "./Icon.stories";

<Meta title="UI Material/Components/Icon" of={IconStories} />

<Title>Icon</Title>

<Subtitle>Definition</Subtitle>

Icon component helps to render [Material UI](https://material-ui.com/components/icons/#icons/) icon.

<Subtitle>Usage</Subtitle>
How to render an icon (template):

```tsx
import { [Icon Name] } from '@material-ui/icons';
import { Icon } from '@reactionable/ui-material/lib/icon/Icon';

const MyIcon = () => (
  <Icon icon={[Icon Name]} />
);
```

<Primary />

<Subtitle>Properties</Subtitle>

<Controls />

<Stories />
