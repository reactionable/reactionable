{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna bootstrap && lerna run --parallel start --",
    "test": "lerna run --stream test --",
    "test:ci": "lerna run --stream test:coverage --",
    "format": "prettier --cache --write '**/*.ts' '!**/lib/**'",
    "lint": "lerna run --stream lint --",
    "lint:ci": "lerna run --stream lint:ci --",
    "build": "lerna run --stream build --",
    "build:ci": "lerna run --stream build:ci --",
    "build:all": "npm run build:ci && npm run docs:build && npm run storybook:build",
    "link": "node dev-link link",
    "unlink": "node dev-link unlink",
    "docs:build": "lerna run --stream build:docs",
    "storybook": "storybook dev -p 6006",
    "storybook:build": "npm run build-storybook -- -o build/storybook",
    "prepare": "ts-dev-tools install",
    "ci": "npm run lint && npm run build:all && npm run test:ci",
    "build-storybook": "storybook build"
  },
  "devDependencies": {
    "@chromatic-com/storybook": "^5.0.0",
    "@jridgewell/gen-mapping": "^0.3.13",
    "@storybook/addon-docs": "^10.2.7",
    "@storybook/addon-links": "^10.2.7",
    "@storybook/addon-onboarding": "^10.2.7",
    "@storybook/react": "^10.2.7",
    "@storybook/react-vite": "^10.2.7",
    "@ts-dev-tools/react": "^1.9.14",
    "barrelsby": "^2.5.1",
    "eslint-plugin-storybook": "^10.2.7",
    "lerna": "^9.0.3",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "remark-gfm": "^4.0.0",
    "rimraf": "^6.1.2",
    "storybook": "^10.2.7",
    "typedoc": "^0.28.16",
    "vite": "^7.3.1"
  },
  "overrides": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "prettier": {
    "semi": true,
    "printWidth": 100,
    "trailingComma": "es5",
    "plugins": []
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional",
      "@commitlint/config-nx-scopes"
    ]
  },
  "lint-staged": {
    "*.{js,ts,tsx}": [
      "eslint --fix"
    ]
  },
  "importSort": {
    ".js, .jsx, .ts, .tsx": {
      "style": "module",
      "parser": "typescript"
    }
  },
  "tsDevTools": {
    "version": "20250623095600-remove-prettier-oxc"
  }
}
